package test23;

public class DB {
	//      구조 	            데이터
	//수정     alter	    update
	//생성     create   insert
	//삭제     drop     delete
	//보기     show     select	
	
	/*drop database red;

	create database oreo;

	use oreo;

	create table user_info(
	uNum int,
	uName varchar(100),
	uAge tinyint,
	uAddress varchar(300),
	uEct varchar(2000)
	);

	select * from user_info;
	insert into user_info
	(uNum, uName, uAge, uAddress, uEct)
	values (2, '임꺽정', 25, '부산', '테스트');
	delete from user_info;

	update user_info
	set uAge = 73,
	uEct = '테스트3'where uName = '홍길동';*/
}
